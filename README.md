# How to run
1. install `uv`
2. run `uv sync` in this directory
3. run `fdtd.py` through one of below methods
    - `source .venv/bin/activate`, then `python fdtd.py`
    - `uv run fdtd.py` (preferred)